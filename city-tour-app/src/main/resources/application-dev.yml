spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/citytourdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    cache: false

server:
  port: 8080

# Для разработки
logging:
  level:
    ru.citytour: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG